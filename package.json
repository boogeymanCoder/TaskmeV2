{
  "name": "taskmev3",
  "version": "1.0.0",
  "description": "One time part-time job hiring site",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "devStart": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "devBuild": "concurrently \"nodemon server.js\" \"cd client && npm install && npm run build\""
  },
  "keywords": [
    "taskme",
    "part-time",
    "job"
  ],
  "author": "cashew",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "bcrypt": "^5.0.1",
    "express": "^4.17.1",
    "express-session": "^1.17.2",
    "mongoose": "^6.0.12",
    "passport": "^0.5.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.14"
  }
}
