#TaskmeV2
Requirements:
  Mongodb Server
  Node.js and npm
To run on local:
  clone repository
  cd to project directory
  run "npm install" to install dependencies
  run "npm run devStart" to run on local
